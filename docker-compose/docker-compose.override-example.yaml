services:

  caddy:
    scale: 0

  sourcegraph-frontend-0:
    ports:
      - '0.0.0.0:7080:3080'

  grafana:
    environment:
      - 'PGHOST=${PGHOST}'
      - 'PGPORT=${PGPORT}'
      - 'PGDATABASE=${PGDATABASE}'
      - 'PGGRAFANAUSER=${PGGRAFANAUSER}'
      - 'PGGRAFANAPASSWORD=${PGGRAFANAPASSWORD}'
      - 'PGSSLMODE=${PGSSLMODE}'
